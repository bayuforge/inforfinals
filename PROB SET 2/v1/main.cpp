/* NOTICE (must read for anyone following my code entirely)
  if you follow my code, iostream are optional. You can comment them. The reason I added it because the exam style 
  one was a mandatory requirement. It acts as a safety guard so you don't actually makes the code error in compilation
  because some people actually puts it without having input output at their 'main.cpp' code. So having to know the 
  reason why you're using them are very important knowledge to know. So you don't just blindly follow some instruction.
*/

#include <iostream> 
#include "nyepi.h"
using std::cout;

int main(){
        input();

        return 0;
}
